import React from 'react';

const Button = ({}) => {
    return (
        <button className='mt-4 px-6 py-4 bg-secondary text-primary border border-primary'>JOIN WAITLIST for exclusive early access</button>
    );
};

export default Button;