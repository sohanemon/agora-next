import React from 'react';

const Button = ({}) => {
    return (
        <button className='mt-4 text-primary bg-secondary border border-primary px-6 py-4'>JOIN WAITLIST for exclusive early access</button>
    );
};

export default Button;